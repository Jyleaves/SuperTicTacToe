# 性能与验证

## 2026-09-08 · v1.1.1：棋力复核

不能用吞吐或单次固定种子结果保证整局棋力不变。新增对弈工具会为每组生成不同种子，
轮换空棋盘及随机走到第 8 / 16 / 24 手的合法开局，并交换双方颜色；双方均复用搜索树。
所有比较保持单线程、相同每步预算，原型筛选后使用独立种子复核。
正式复核共 280 局，包含 140 组开局种子；已核验 280 条对弈轨迹均不同。

### 结论与改动

- 相同节点容量时，原项目与 v1.1.0 优化内核在 690 次连续搜索中的落子、统计和迭代数相同。
  缩小节点池的另一组 328 次搜索有 300 次结果不同，说明整局树复用不能由单次等价性推断。
- AI 节点池恢复原项目的 524,288 个节点，避免内存优化提前丢弃搜索统计；显示用评估仍按工作量分配；大师档原本已使用该容量。
- 求胜时保留直接制胜优先，并在有安全候选时排除允许对手下一步赢整盘的落子。
  强制区域、已结束大格、求败模式与所有候选都不安全时的回退均有测试。
- 合法 43 手棋谱、种子 `8820990395033202828`、2,000 次搜索已复现实际改进：
  原最终选步允许对手一步制胜，新选步避免该次失利。棋谱在 `tests/fixtures/avoid_immediate_loss.txt`。
  这证明局部战术修复，不证明最终一定获胜。
- 原型筛选中，按访问次数选步为 33 胜 / 54 和 / 33 负；按平均收益为 36 / 57 / 27，均为 120 局。
  未据此采用新的最终评分规则，也不宣称整体棋力提升。

### 对弈数据

下表胜 / 和 / 负均从候选版视角统计。得分率 = (胜 + 和 / 2) / 总局数。
“原选步”指保留原 UCB 最终选步的同容量搜索；前两行隔离战术检查，第三行也包含节点容量变化。

| 对照 | 每步预算 | 局数 | 胜 / 和 / 负 | 得分率 |
|---|---|---:|---|---:|
| 原选步，同容量 | 8,000 次 | 160 | 45 / 70 / 45 | 50.0% |
| 原项目，同容量 | 32,000 次 | 80 | 20 / 40 / 20 | 50.0% |
| v1.1.0，正式复核 | 8,000 次 | 160 | 44 / 59 / 57 | 45.9% |
| v1.1.0，正式复核 | 12 ms，最多 2,000,000 次 | 120 | 37 / 52 / 31 | 52.5% |

正式等迭代复核的种子为 `60101`，按开局配对 bootstrap 的 95% 区间为 40.0%–51.6%。
等时间复核的种子为 `70101`，95% 区间为 46.3%–58.8%。
两组结果都不足以排除棋力下降，也不支持提升；恢复容量是保留原项目复用行为的选择，不能视作已证实更强。
比较仅覆盖指定开局和预算，没有人类棋手或外部强引擎评级，不换算 Elo。

复现入口见 README 的“棋力对照”。`--time-ms` 的预算在每 512 次迭代检查，不能当作严格硬截止时间。
这里直接测试 Rust 搜索，不含 GUI、显示用评估或时间随机种子的分配方式。

本版回归：Rust 默认与强制软件回退各 31 项，包含实际一步失利棋谱、随机棋盘规则对照及精确树复用检查。

Python 43 项、Node 13 项、DLL 桥、100 局规则等价性和真实 WebView 冒烟均通过。

较低难度的 AI 内存占用可能高于 v1.1.0；下方 v1.1.0 工作集数据是该历史版本的测量，不能直接代表本版。
锁外搜索、取消机制、CPU 运行时分派及前端响应优化继续保留。

## 2026-09-08 · v1.1.0

基于 `aafbc23` 审查并集成性能修正包，补充本机 Windows 验证和优化。
保留 MCTS 选步规则、greedy-1 模拟策略、难度档位与单树 AI。

### 搜索耗时

同一台 Intel Core i7-13700H、Windows x64、Rust 1.92.0；单线程，固定逻辑核心 0；
每个局面 128,000 次迭代，一次预热后交替运行 7 轮，取中位数。
原项目按原构建配置启用全局 CPU 指令；修正包和最终版按各自默认配置构建。
下表只计搜索时间，完整调用时间可用 `tests/compare_search.py` 重测。

| 合法棋谱 | 原项目 | 修正包 | 最终版 | 最终版相对原项目 |
|---|---:|---:|---:|---:|
| 开局 · 0 手 | 416.7 ms | 548.5 ms | 404.8 ms | 耗时减少 2.9% |
| 中局 · 28 手 | 257.3 ms | 331.4 ms | 197.7 ms | 耗时减少 23.2% |
| 残局 · 56 手 | 12.7 ms | 11.3 ms | 12.8 ms | 未见稳定提升 |

相对修正包，最终版开局、中局耗时分别减少 26.2%、40.3%。
这三个固定局面不能代表所有对局；残局含直接制胜机会，不能据此推断一般残局吞吐。
混合核心调度、电源状态、温度及后台负载会显著影响结果，未绑定核心的数据不与本表混用。
旧文档的中局/残局棋谱含非法步，相关吞吐及消融结论不再作为当前性能依据。

### 响应与资源

`tests/bench_session.py` 在三个独立 Python/DLL 进程中各运行 3 次大师开局，
关闭胜率条，并在 AI 开始后 40 ms 查询状态；未绑定核心，取延迟中位数。
工作集只含 Python/DLL 进程，不含 WebView2，也不是峰值内存。

| 指标 | 原项目 | 修正包 | 最终版 |
|---|---:|---:|---:|
| 搜索期间查询状态 | 645.5 ms | 0.024 ms | 0.020 ms |
| 大师开局 AI 调用 | 686.0 ms | 1,035.6 ms | 683.6 ms |
| 测试结束工作集 | 71.9 MiB | 45.6 MiB | 45.5 MiB |

原项目即使关闭胜率条仍启动评估；修正包和最终版会停止该工作。
最终版还按难度分配节点池，并在 AI 搜索期间取消显示用评估。
内存收益因难度和是否启用评估而异；上表不代表所有模式。
开启胜率时，同一脚本另测修正包 / 最终版：AI 调用 815.6 / 615.0 ms，工作集 90.5 / 84.6 MiB。

### 启动与分发

最终 EXE 的诊断入口实测：500 次 IPC ping 平均 0.392 ms，单次启动到 DOM 就绪 1.141 s；
这些是运行检查，没有同条件的原版对照，不宣称启动或桥延迟加速。
EXE 内 1 / 8 线程共 6 项引擎基准均正常完成。
发布 EXE 为 936,448 字节，比 v1.0.0 Release 的 1,249,792 字节减少 25.1%。
EXE / DLL 的 UTF-8、UTF-16 本机路径扫描通过，不含构建机器的项目目录或用户目录。

### 实现

- 会话搜索移到锁外，按版本提交；过期搜索在每 512 次迭代的批次边界取消。
- 评估采用常驻协调线程、最新任务队列和可复用节点池，关闭/离开/终局时取消。
- 1 KiB 缺口表替代模拟初始化时的重复棋线扫描；残局跳过不会使用的缺口初始化。
- x64 搜索批次运行时选择 BMI2 / POPCNT / LZCNT 内核，其余 CPU 使用软件路径。
- 前端串行化新局创建，过滤过期回包，复用棋盘 DOM，支持键盘操作、减少动效和请求重试。
- 构建统一 Release 配置，固定产物复制目录，映射源码路径并移除调试符号；CI 验证桌面 EXE 和 DLL，上传可下载产物。

### 已验证

- Rust 默认与强制软件回退：各 28 项通过。
- 缺口表：全部 19,683 种无重叠小棋盘状态，在 9 个大格偏移上与参考扫描一致。
- 固定随机种子：3 局面 × 2 目标 × 3 种子，18 组搜索的落子、统计和迭代数与修正包完全一致。
- Python：规则、AI、数组搜索、参照算法和基准棋谱共 43 项通过。
- Node：13 项异步前端回归通过。
- Rust/Python：300 局逐步比对棋盘、合法步、轮次、强制区域与终局通过；DLL 桥冒烟通过。
- 真实 pywebview 窗口：开局、落子、AI、认输、重开、设置、菜单、规则翻页通过。
- 原生 Rust 窗口：大师电脑先手、叉号回合图标、Tab/Enter 落子与 ESC 返回菜单检查通过。
- Rust 格式化与 Clippy 检查通过；文档使用相对项目路径，发布前扫描路径和凭据格式。

### 后续方向与边界

1. 在不同 Intel/AMD CPU 上重复测量，尤其核验老 CPU 的软件回退及混合核心调度。本次未做其他实体 CPU 或 ARM64 实机验证。
2. 多线程根投票与单树的最终选步规则不同；若改 AI 并行策略，先统一策略并进行等时间、多种子对弈。不能用迭代吞吐替代棋力证据。
3. 更复杂的树压缩、残局求解或 rollout 策略可能改变结果，需要单独的策略验证。本次保留原策略。
4. 本次没有加入 GPU 路径；这需要重新设计批量模拟与数据布局，现有数据不足以证明收益。
