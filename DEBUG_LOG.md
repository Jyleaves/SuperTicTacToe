# 修复记录

当前实现与验证入口；早期开发流水记录保留在 Git 历史中。

| 问题 | 修复 | 验证 |
|---|---|---|
| 展开节点重复计算合法步，遍历重复检查终局 | 保存剩余合法步和终局结果，移除两组计数数组 | 子节点不重不漏、随机棋盘规则对照、跨版本整局一致性 |
| 模拟每步重复扫描棋线、检查 CPU 指令支持 | 小棋盘胜负与缺口查表，批次入口选择 CPU 内核 | 查表穷举、默认/软件回退、多线程一致性与扩展基准 |
| 缩小 AI 节点池提前丢弃复用树，单次搜索测试不能验证此变化 | 恢复原 AI 容量，补充多种子、交换颜色的对弈工具 | `tests/duel_versions.py`、精确树复用回归 |
| 最终探索奖励可能选中允许对手一步制胜的走法 | 求胜时排除可避免的一步失利，保留原树内搜索与求败行为 | Rust 43 手合法棋谱回归、200 局随机棋盘规则对照 |
| Python 树复用测试假设短限时内已展开指定回复，偶发失败 | 固定迭代，复用实际展开的子树，断言精确访问次数增长 | `tests/test_ai.py` |
| AI 持有会话锁，查询、认输等待搜索结束 | 搜索移到锁外，以版本校验提交结果，批次间取消 | Rust 会话并发测试、`tests/bench_session.py` |
| 每步创建评估线程，旧任务排队耗费 CPU | 常驻协调线程、仅保留最新任务；关闭评估、终局、离开时取消 | Rust 会话测试、桥冒烟 |
| 快速点击重复落子，旧回包覆盖新局 | 立即锁定前端输入；版本、对局编号和前端 epoch 校验 | `tests/test_frontend.cjs` |
| 并发新局响应可能携带其他对局编号 | 创建与状态快照在同一临界区完成 | Rust 并发新局测试 |
| 桥调用失败只写控制台，无法继续 | 展示简短提示及重试入口，释放输入和思考状态 | 前端失败重试回归 |
| 重开仍显示上局胜率 | 新局清空旧统计 | 前端回归 |
| 老基准中局、残局含非法棋步 | 使用可逐步验证的 0 / 28 / 56 手公共棋谱 | Rust / Python 基准棋谱测试 |
| 软件回退拖慢搜索，全局高级指令又限制 CPU 兼容性 | 运行时分派搜索内核，保留可强制测试的软件路径 | `portable` 测试及固定种子回归 |
| 模拟重复扫描同一棋线 | 1 KiB 缺口表，枚举验证全部小棋盘状态与跨字边界 | Rust 查表穷举测试 |
| 发布 EXE 中嵌入本机源码路径 | 构建时映射项目和 Cargo 路径，移除调试符号 | 最终 EXE/DLL 字节扫描 |
| DLL 复制路径指向错误目录 | 构建脚本固定两个产物目录，并检查复制结果 | `rust/build.cmd` |
| GUI 脚本只有输出，没有失败断言 | 断言交互结果，失败返回非零，关闭超时看门狗 | `tests/smoke_gui.py` |

## 排查

- Python 提示缺少 DLL 或接口不存在：重新运行 `rust/build.cmd`，保证源码与 DLL 同一版本。
- 窗口无法初始化：安装 WebView2 Runtime。安装 Edge 浏览器不能替代检查 Runtime。
- 性能结果波动：停止其他重负载，保持电源状态一致，使用同一核心、棋谱、迭代数和计时边界做交替测量。
- 提交问题时描述版本、系统、CPU、复现步骤；去掉日志中的本机路径、账户标识及凭据。
